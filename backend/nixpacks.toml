[providers]
providers = ["java"]

[phases.setup]
nixPkgs = ["openjdk21", "maven"]

[phases.build]
cmds = ["mvn clean package -DskipTests"]

[start]
cmd = "java -Dserver.port=$PORT -jar target/bytepe-backend-1.0.0.jar"
